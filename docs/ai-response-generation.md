# Генерация ответов ChatGPT для пользователей

## 🎯 Как это работает

Система автоматически генерирует ответы на вопросы пользователей с помощью ChatGPT:

1. **При создании заявки:**
   - Сначала проверяется база знаний FAQ
   - Если есть совпадение - используется ответ из FAQ (улучшенный ChatGPT)
   - Если нет совпадения - ChatGPT генерирует новый ответ на основе вопроса

2. **Автоматический ответ:**
   - Если уверенность > 70% - ответ автоматически отправляется пользователю
   - Тикет может быть автоматически закрыт (если уверенность > 80%)

## 🔧 Техническая реализация

### Функция генерации ответа

```typescript
AIService.generateUserResponse(question, context?)
```

**Параметры:**
- `question` - вопрос пользователя
- `context` (опционально) - контекст тикета (категория, тип, отдел)

**Возвращает:**
```typescript
{
  answer: string;           // Сгенерированный ответ
  confidence: number;        // Уверенность (0-1)
  shouldCloseTicket: boolean; // Закрыть ли тикет автоматически
}
```

### API Endpoint

```
POST /api/ai/generate-answer
```

**Запрос:**
```json
{
  "question": "Как сбросить пароль?",
  "context": {
    "category": "Техническая проблема",
    "type": "question",
    "department": "Техническая поддержка"
  }
}
```

**Ответ:**
```json
{
  "response": {
    "answer": "Для сброса пароля перейдите на страницу входа...",
    "confidence": 0.85,
    "shouldCloseTicket": true
  }
}
```

## 📝 Примеры использования

### Пример 1: Вопрос из FAQ

**Вопрос:** "Как сбросить пароль?"

**Процесс:**
1. Найдено совпадение в FAQ
2. ChatGPT улучшает ответ из FAQ
3. Ответ отправляется пользователю
4. Тикет закрывается автоматически

### Пример 2: Новый вопрос

**Вопрос:** "Как настроить двухфакторную аутентификацию?"

**Процесс:**
1. Нет совпадения в FAQ
2. ChatGPT генерирует ответ на основе вопроса
3. Ответ отправляется пользователю
4. Тикет может быть закрыт (если уверенность высокая)

### Пример 3: Сложный вопрос

**Вопрос:** "У меня проблема с интеграцией API, ошибка 500"

**Процесс:**
1. Нет точного совпадения в FAQ
2. ChatGPT анализирует вопрос и генерирует ответ с шагами решения
3. Если уверенность низкая - тикет остается открытым для оператора

## ⚙️ Настройка

### 1. API ключ OpenAI

Создайте файл `.env.local`:

```env
OPENAI_API_KEY=sk-your-api-key-here
```

**Важно:** Никогда не храните API ключ в коде! Используйте переменные окружения.

### 2. Модель ИИ

По умолчанию используется `gpt-4o-mini`. Чтобы изменить:

```typescript
// lib/ai-service.ts
const response = await openai.chat.completions.create({
  model: 'gpt-4o-mini', // Измените здесь
  // ...
});
```

### 3. Пороги уверенности

Настройте когда автоматически закрывать тикеты:

```typescript
// lib/ai-service.ts
shouldCloseTicket: confidence > 0.7  // Измените порог
```

## 🎨 Кастомизация промптов

### Промпт для генерации ответов

Можно изменить промпт в `lib/ai-service.ts`:

```typescript
const prompt = `Ты помощник службы поддержки. 
Пользователь задал вопрос: "${question}"

// Добавьте свои инструкции здесь
// Например, стиль ответа, формат, и т.д.
`;
```

### Примеры кастомизации

**Более формальный стиль:**
```typescript
content: 'Ты профессиональный оператор службы поддержки. Отвечай формально и официально.'
```

**Более дружелюбный стиль:**
```typescript
content: 'Ты дружелюбный помощник. Отвечай неформально, но профессионально.'
```

## 🔍 Логика работы

### Приоритет ответов:

1. **FAQ с совпадением** → Используется ответ из FAQ (улучшенный ChatGPT)
2. **Нет совпадения + есть API ключ** → ChatGPT генерирует новый ответ
3. **Нет совпадения + нет API ключа** → Тикет остается открытым

### Определение уверенности:

- **Высокая (0.8-1.0):** Точное совпадение в FAQ или четкий вопрос
- **Средняя (0.6-0.8):** Похожий вопрос или общий ответ
- **Низкая (<0.6):** Неясный вопрос, требуется помощь оператора

## 🚀 Улучшения

### 1. Кэширование ответов

Можно кэшировать похожие вопросы:

```typescript
// Сохранять ответы для похожих вопросов
// Использовать векторный поиск для похожих вопросов
```

### 2. Обучение на исторических ответах

```typescript
// Анализировать успешные ответы операторов
// Использовать их как примеры для ChatGPT
```

### 3. Мультиязычная поддержка

```typescript
// Определять язык вопроса
// Генерировать ответ на том же языке
```

## 📊 Мониторинг

### Логирование

Все запросы к ChatGPT логируются:

```typescript
console.error('AI response generation error:', error);
```

### Метрики

Можно отслеживать:
- Количество автоматических ответов
- Уверенность ответов
- Процент закрытых тикетов

## 🔒 Безопасность

1. **API ключ в переменных окружения** - никогда в коде
2. **Валидация входных данных** - проверка на вредоносный контент
3. **Ограничение токенов** - `max_tokens: 500` предотвращает длинные ответы
4. **Rate limiting** - ограничение количества запросов

## 💡 Советы

1. **Настройте температуру** - для более креативных ответов увеличьте `temperature`
2. **Добавьте контекст** - передавайте больше информации о тикете
3. **Тестируйте промпты** - экспериментируйте с разными формулировками
4. **Мониторьте качество** - проверяйте сгенерированные ответы

