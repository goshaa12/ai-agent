# Настройка ChatGPT для подсказок операторам

## 🚀 Быстрая настройка

### 1. Получите API ключ OpenAI

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый API ключ
4. Скопируйте ключ (он показывается только один раз!)

### 2. Настройте переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
OPENAI_API_KEY=sk-your-api-key-here
```

**Важно:** 
- Не коммитьте `.env.local` в git (он уже в .gitignore)
- Используйте `.env.local` для локальной разработки
- Для продакшена настройте переменные окружения на вашем хостинге

### 3. Перезапустите сервер

```bash
# Остановите сервер (Ctrl+C) и запустите снова
bun dev
```

## ✨ Как это работает

### Автоматическая генерация подсказок

При открытии тикета ChatGPT автоматически:
1. Анализирует описание проблемы
2. Изучает историю переписки
3. Учитывает контекст (категория, приоритет, тип, отдел)
4. Генерирует 3 варианта профессиональных ответов

### Умные подсказки

ChatGPT учитывает:
- **Контекст тикета**: категория, приоритет, тип, отдел
- **Историю переписки**: все предыдущие сообщения
- **Стиль ответа**: 
  - Для срочных вопросов - более оперативный тон
  - Для технических проблем - конкретные шаги решения
  - Для вопросов - развернутые ответы

### Использование подсказок

1. Откройте тикет
2. ChatGPT автоматически сгенерирует подсказки
3. Нажмите на любую подсказку, чтобы использовать её как ответ
4. При необходимости нажмите "Обновить" для новых вариантов

## 🔧 Технические детали

### Модель ИИ

Система использует `gpt-4o-mini` - оптимизированную модель OpenAI:
- Быстрая генерация ответов
- Низкая стоимость
- Хорошее качество для задач поддержки

### Промпт для генерации

ChatGPT получает детальный промпт с:
- Описанием проблемы
- Историей переписки (последние 8 сообщений)
- Контекстом тикета (категория, приоритет, тип, отдел)
- Требованиями к стилю ответа

### API Endpoint

```
POST /api/ai/generate-responses
```

**Параметры:**
```json
{
  "ticketDescription": "Описание проблемы",
  "conversationHistory": ["История сообщений"],
  "count": 3,
  "ticketContext": {
    "title": "Тема",
    "category": "Категория",
    "priority": "Приоритет",
    "type": "Тип",
    "department": "Отдел"
  }
}
```

## 💡 Примеры использования

### Пример 1: Техническая проблема

**Заявка:** "Не могу войти в систему, ошибка 500"

**ChatGPT предложит:**
1. "Здравствуйте! Понимаю вашу проблему. Давайте проверим несколько моментов: очистите кэш браузера, попробуйте другой браузер или режим инкогнито. Если проблема сохранится, пришлите скриншот ошибки."
2. "Добрый день! Ошибка 500 обычно связана с проблемами на сервере. Попробуйте обновить страницу через несколько минут. Если проблема не решится, мы проверим логи на нашей стороне."
3. "Привет! Для быстрого решения попробуйте: 1) Очистить cookies, 2) Использовать другой браузер, 3) Проверить интернет-соединение. Если ничего не поможет, мы разберемся с технической стороны."

### Пример 2: Вопрос о продукте

**Заявка:** "Хочу узнать о ценах на продукт X"

**ChatGPT предложит:**
1. "Здравствуйте! Спасибо за интерес к нашему продукту X. Цены зависят от выбранного тарифа. Могу предложить несколько вариантов, подходящих под ваши задачи. Какой объем использования планируете?"
2. "Добрый день! Рад помочь с информацией о продукте X. У нас есть несколько тарифных планов. Чтобы подобрать оптимальный вариант, уточните, пожалуйста, планируемый объем использования и основные задачи."
3. "Привет! Продукт X доступен в нескольких тарифах. Для точного расчета рекомендую обсудить ваши требования. Могу предложить бесплатную консультацию, чтобы подобрать оптимальное решение."

## 🛠️ Troubleshooting

### Подсказки не генерируются

1. **Проверьте API ключ:**
   ```bash
   # Убедитесь, что файл .env.local существует и содержит ключ
   cat .env.local
   ```

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Посмотрите ошибки в консоли
   - Проверьте Network tab на наличие ошибок API

3. **Проверьте логи сервера:**
   - Ошибки будут видны в терминале, где запущен `bun dev`

### Ошибка "API key not found"

- Убедитесь, что файл `.env.local` создан в корне проекта
- Перезапустите сервер после добавления ключа
- Проверьте, что ключ начинается с `sk-`

### Ошибка "Insufficient quota"

- У вас закончились средства на счету OpenAI
- Пополните баланс на [platform.openai.com](https://platform.openai.com/account/billing)

### Медленная генерация

- Это нормально для первого запроса (холодный старт)
- Последующие запросы будут быстрее
- Можно использовать более быструю модель (но менее качественную)

## 📊 Стоимость

`gpt-4o-mini` - очень экономичная модель:
- ~$0.15 за 1M входных токенов
- ~$0.60 за 1M выходных токенов
- Один запрос на генерацию подсказок ≈ $0.001-0.002

## 🔒 Безопасность

- **Никогда не коммитьте API ключи в git**
- Используйте переменные окружения
- Для продакшена используйте секреты хостинга (Vercel, Railway и т.д.)
- Ограничьте доступ к API ключу через настройки OpenAI

## 🎯 Дальнейшие улучшения

Можно улучшить систему:
1. Кэширование подсказок для похожих тикетов
2. Обучение на исторических ответах операторов
3. Интеграция с базой знаний компании
4. Мультиязычная поддержка

