# Процесс классификации и сортировки заявок

## 📋 Общий процесс

Когда пользователь создает заявку, система автоматически анализирует текст и классифицирует его по нескольким параметрам:

```
Заявка пользователя
    ↓
Анализ текста ИИ (OpenAI GPT)
    ↓
Определение параметров:
  - Категория
  - Приоритет
  - Тип
  - Отдел
    ↓
Создание тикета с назначенными параметрами
    ↓
Автоматическая маршрутизация в нужный отдел
```

## 🔍 Детальный процесс классификации

### 1. Анализ текста заявки

**Файл:** `lib/ai-service.ts` → метод `analyzeTicket()`

Система анализирует текст заявки (заголовок + описание) и определяет:

#### a) Категория
Краткое описание проблемы, например:
- "Техническая проблема"
- "Вопрос о продажах"
- "Финансовый вопрос"
- "HR вопрос"

#### b) Приоритет (4 уровня)
- **`urgent`** - Срочный (критичные проблемы, влияющие на бизнес)
- **`high`** - Высокий (важные вопросы)
- **`medium`** - Средний (по умолчанию)
- **`low`** - Низкий (не срочные вопросы)

#### c) Тип заявки (5 типов)
- **`question`** - Вопрос
- **`issue`** - Проблема/баг
- **`request`** - Запрос
- **`complaint`** - Жалоба
- **`feedback`** - Отзыв

#### d) Отдел (5 отделов)
- **`tech`** - Техническая поддержка
- **`sales`** - Отдел продаж
- **`billing`** - Бухгалтерия
- **`hr`** - HR
- **`general`** - Общие вопросы

### 2. Как работает ИИ классификация

#### С OpenAI API (если настроен ключ):

```typescript
// ИИ получает промпт с описанием всех отделов
const prompt = `Проанализируй заявку и определи:
1. Категорию
2. Приоритет (low, medium, high, urgent)
3. Тип (question, issue, request, complaint, feedback)
4. Отдел из списка: [список отделов]

Заявка: "${text}"

Ответь в формате JSON: {...}`;
```

ИИ анализирует контекст, семантику и определяет параметры.

#### Fallback логика (без OpenAI):

Если API ключ не настроен, используется эвристика на основе ключевых слов:

**Техническая поддержка (`tech`):**
- Ключевые слова: "пароль", "войти", "логин", "не работает", "ошибка", "баг"
- Тип: `issue`
- Категория: "Техническая проблема"

**Отдел продаж (`sales`):**
- Ключевые слова: "купить", "цена", "заказ", "товар", "продаж"
- Тип: `question`
- Категория: "Вопрос о продажах"

**Бухгалтерия (`billing`):**
- Ключевые слова: "оплата", "счет", "деньги", "возврат", "refund"
- Тип: `request`
- Категория: "Финансовый вопрос"

**HR (`hr`):**
- Ключевые слова: "отпуск", "документ", "кадр"
- Тип: `question`
- Категория: "HR вопрос"

**Общие вопросы (`general`):**
- По умолчанию, если не подходит ни один отдел

**Определение приоритета (fallback):**
- `urgent`: "срочно", "urgent", "критично"
- `high`: "важно", "important"
- `low`: "неважно", "когда будет время"
- `medium`: по умолчанию

### 3. Автоматическая маршрутизация

После классификации система:

1. **Создает тикет** с определенными параметрами
2. **Назначает отдел** автоматически
3. **Устанавливает приоритет** для сортировки
4. **Проверяет FAQ** для автоматического ответа

### 4. Примеры классификации

#### Пример 1: Техническая проблема
```
Заявка: "Не могу войти в систему, выдает ошибку 500"
→ Отдел: tech (Техническая поддержка)
→ Приоритет: high
→ Тип: issue
→ Категория: Техническая проблема
```

#### Пример 2: Вопрос о продажах
```
Заявка: "Хочу узнать цену на продукт X"
→ Отдел: sales (Отдел продаж)
→ Приоритет: medium
→ Тип: question
→ Категория: Вопрос о продажах
```

#### Пример 3: Финансовый вопрос
```
Заявка: "СРОЧНО! Нужен возврат денег за заказ #12345"
→ Отдел: billing (Бухгалтерия)
→ Приоритет: urgent
→ Тип: request
→ Категория: Финансовый вопрос
```

## 🎯 Где используется классификация

1. **Создание тикета** (`app/api/tickets/create/route.ts`)
   - Анализ выполняется при создании
   - Результаты сохраняются в тикете

2. **Фильтрация в UI** (`app/page.tsx`)
   - Пользователь может фильтровать по отделам
   - Сортировка по дате создания (новые сверху)

3. **Отображение** (`components/TicketCard.tsx`)
   - Показываются: отдел, категория, приоритет, статус

## 📊 Структура данных тикета

```typescript
interface Ticket {
  id: string;
  title: string;
  description: string;
  category: string;           // Определяется ИИ
  priority: TicketPriority;   // Определяется ИИ
  type: TicketType;           // Определяется ИИ
  departmentId: string;        // Определяется ИИ
  departmentName: string;       // Определяется ИИ
  status: TicketStatus;
  // ...
}
```

## 🔧 Настройка отделов

Отделы настраиваются в `lib/departments.ts`:

```typescript
export const departments: Department[] = [
  {
    id: 'tech',
    name: 'Техническая поддержка',
    description: 'Проблемы с программным обеспечением, оборудованием, сетью'
  },
  // ...
];
```

Чтобы добавить новый отдел:
1. Добавьте его в массив `departments`
2. ИИ автоматически начнет его использовать
3. Обновите fallback логику в `fallbackAnalysis()` если нужно

## 💡 Улучшение классификации

Для более точной классификации:

1. **Настройте OpenAI API ключ** - ИИ будет анализировать контекст, а не только ключевые слова
2. **Расширьте fallback логику** - добавьте больше ключевых слов в `fallbackAnalysis()`
3. **Добавьте примеры** - можно использовать few-shot learning для улучшения точности

