# 🤖 AI Система поддержки

Интеллектуальная система обработки заявок с использованием OpenAI GPT для автоматической классификации, маршрутизации и ответов на вопросы.

## ✨ Возможности

### 1. Анализ заявок с помощью ИИ
- **Автоматическая классификация**: ИИ определяет категорию, приоритет, тип и отдел для каждой заявки
- **Умная маршрутизация**: Заявки автоматически направляются в нужный отдел
- **Определение приоритета**: Система оценивает срочность и важность заявки

### 2. Автоматические ответы
- **База знаний FAQ**: Система ищет ответы в базе знаний
- **Автоматические ответы**: При высокой уверенности ИИ сразу отвечает пользователю
- **Автозакрытие тикетов**: Простые вопросы закрываются автоматически

### 3. Помощь операторам
- **Генерация вариантов ответов**: ИИ предлагает несколько вариантов профессиональных ответов
- **Резюмирование переписки**: Автоматическое создание краткого резюме длинных переписок
- **Перевод между языками**: Перевод сообщений для международной поддержки

## 🚀 Быстрый старт

### Установка зависимостей

```bash
bun install
```

### Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

**Примечание**: Система работает и без API ключа OpenAI, используя fallback логику на основе ключевых слов. Однако для полной функциональности рекомендуется добавить ключ.

### Запуск сервера разработки

```bash
bun dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
ai-agent/
├── app/
│   ├── api/
│   │   ├── tickets/          # API для работы с тикетами
│   │   └── ai/                # API для ИИ функций
│   ├── page.tsx              # Главная страница
│   └── layout.tsx            # Layout приложения
├── components/
│   ├── TicketForm.tsx        # Форма создания заявки
│   ├── TicketCard.tsx         # Карточка тикета
│   └── TicketDetail.tsx      # Детальный просмотр тикета
├── lib/
│   ├── ai-service.ts         # Сервис для работы с OpenAI
│   ├── ticket-store.ts       # Хранилище тикетов (in-memory)
│   ├── departments.ts        # Список отделов
│   └── faq.ts                # База знаний FAQ
└── types/
    └── index.ts              # TypeScript типы
```

## 🔧 API Endpoints

### Тикеты

- `POST /api/tickets/create` - Создание нового тикета с автоматическим анализом
- `GET /api/tickets` - Получение списка тикетов
- `GET /api/tickets/[id]` - Получение тикета по ID
- `PATCH /api/tickets/[id]` - Обновление тикета
- `POST /api/tickets/[id]/messages` - Добавление сообщения в тикет

### ИИ функции

- `POST /api/ai/analyze` - Анализ текста заявки
- `POST /api/ai/faq` - Поиск ответа в FAQ
- `POST /api/ai/generate-responses` - Генерация вариантов ответов
- `POST /api/ai/summarize` - Резюмирование переписки
- `POST /api/ai/translate` - Перевод текста

## 🎯 Использование

### Создание заявки

1. Нажмите "Создать заявку"
2. Заполните тему и описание проблемы
3. Система автоматически:
   - Определит категорию и приоритет
   - Направит в нужный отдел
   - Попытается найти ответ в FAQ
   - Если уверенность высокая - автоматически ответит и закроет тикет

### Работа оператора

1. Откройте список заявок
2. Выберите заявку для просмотра
3. Используйте кнопку "🤖 AI Помощник" для:
   - Генерации вариантов ответов
   - Создания резюме переписки
   - Перевода сообщений
4. Выберите один из предложенных ответов или напишите свой
5. Измените статус тикета при необходимости

## 🗄️ База знаний FAQ

FAQ хранится в `lib/faq.ts`. Вы можете добавить новые вопросы и ответы:

```typescript
{
  id: 'faq-id',
  question: 'Вопрос',
  answer: 'Ответ',
  category: 'tech',
  keywords: ['ключевое', 'слово'],
  language: 'ru'
}
```

## 🏢 Отделы

Отделы настраиваются в `lib/departments.ts`:

- **tech** - Техническая поддержка
- **sales** - Отдел продаж
- **billing** - Бухгалтерия
- **hr** - HR
- **general** - Общие вопросы

## 🔄 Хранилище данных

По умолчанию используется in-memory хранилище (`lib/ticket-store.ts`). Для продакшена рекомендуется заменить на реальную базу данных (PostgreSQL, MongoDB и т.д.).

## 🛠️ Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - Типизация
- **OpenAI API** - ИИ функциональность
- **Tailwind CSS** - Стилизация

## 📝 Лицензия

MIT
